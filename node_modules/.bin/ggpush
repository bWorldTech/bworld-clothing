#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../ggpush/ggpush.sh" "$@"
  ret=$?
else 
  sh  "$basedir/../ggpush/ggpush.sh" "$@"
  ret=$?
fi
exit $ret
